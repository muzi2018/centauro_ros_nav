global_costmap:
  update_frequency: 2.0
  publish_frequency: 1.0 #0
  #static_map: false #False if not using an existing map
  #rolling_window: true
  width: 15.0
  height: 15.0
  resolution: 0.1
  origin_x: -10.0
  origin_y: -10.0

  #footprint: [[0.4, 0.29], [0.4, -0.29], [-0.4, -0.29], [-0.4 ,0.29]]
  footprint_padding: 0.30

  plugins:
    - {name: static_layer,       type: "costmap_2d::StaticLayer"}
    - {name: obstacles_layer, type: "costmap_2d::ObstacleLayer"}
    - {name: inflater_layer, type: "costmap_2d::InflationLayer"}

  static_layer:
    map_topic: /projected_map
    subscribe_to_updates: true

  obstacles_layer:
    footprint_clearing_enabled: true
    observation_sources: velodyne
    velodyne: {sensor_frame: head_down, data_type: PointCloud2, topic: /velodyne_points_filtered, marking: true, clearing: true}

  inflater_layer:
    inflation_radius: 1.5
    cost_scaling_factor: 10.0
